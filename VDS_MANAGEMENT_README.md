# 🖥️ Управление Family Finance Bot на VDS сервере

> Полный набор инструментов для управления, мониторинга и автоматизации бота на VDS

## 📚 Документация

### 🎯 Для быстрого старта
- **[CHEATSHEET_RU.md](CHEATSHEET_RU.md)** - Краткая шпаргалка с основными командами ⭐
- **[QUICK_REFERENCE.md](QUICK_REFERENCE.md)** - Быстрая справка по всем методам управления

### 📖 Подробные руководства
- **[VDS_MANAGEMENT_GUIDE.md](VDS_MANAGEMENT_GUIDE.md)** - Полное руководство по всем методам управления
- **[AUTOMATION_SETUP.md](AUTOMATION_SETUP.md)** - Настройка автоматизации и мониторинга

### 📋 Другие полезные документы
- **[DEPLOYMENT_GUIDE.md](family_finance_bot/DEPLOYMENT_GUIDE.md)** - Руководство по развертыванию
- **[README.md](family_finance_bot/README.md)** - Основная документация проекта

---

## 🛠️ Готовые скрипты управления

Все скрипты находятся в корневой директории проекта и готовы к использованию:

### Основные команды

| Скрипт | Описание | Использование |
|--------|----------|---------------|
| `START_BOT.sh` | Запуск бота | `./START_BOT.sh` |
| `STOP_BOT.sh` | Остановка бота | `./STOP_BOT.sh` |
| `RESTART_BOT.sh` | Перезапуск бота | `./RESTART_BOT.sh` |
| `STATUS_BOT.sh` | Проверка статуса | `./STATUS_BOT.sh` |
| `VIEW_LOGS.sh` | Просмотр логов | `./VIEW_LOGS.sh` |

### Дополнительные инструменты

| Скрипт | Описание | Использование |
|--------|----------|---------------|
| `HEALTH_CHECK.sh` | Автоматическая проверка работоспособности | `./HEALTH_CHECK.sh` |

**Особенности скриптов:**
- ✅ Автоматически определяют метод запуска (systemd/Docker/screen/tmux)
- ✅ Интерактивные меню для выбора опций
- ✅ Подробные сообщения о статусе операций
- ✅ Готовы к использованию в cron для автоматизации

---

## 🚀 Быстрый старт

### Шаг 1: Сделайте скрипты исполняемыми

```bash
chmod +x START_BOT.sh STOP_BOT.sh RESTART_BOT.sh STATUS_BOT.sh VIEW_LOGS.sh HEALTH_CHECK.sh
```

### Шаг 2: Выберите метод запуска

#### Вариант A: Systemd Service (Рекомендуется) ⭐

**Преимущества:**
- Автозапуск при перезагрузке сервера
- Автоматический перезапуск при сбоях
- Централизованное логирование
- Простое управление

**Установка:**
```bash
cd family_finance_bot

# Отредактируйте пути в service файле
sudo nano family-finance-bot.service

# Установите service
sudo cp family-finance-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable family-finance-bot
sudo systemctl start family-finance-bot

# Проверьте статус
sudo systemctl status family-finance-bot
```

**Управление:**
```bash
sudo systemctl start|stop|restart|status family-finance-bot
sudo journalctl -u family-finance-bot -f  # Просмотр логов
```

#### Вариант B: Docker Compose 🐳

**Преимущества:**
- Изолированная среда
- Встроенная PostgreSQL
- Легко переносить между серверами

**Использование:**
```bash
cd family_finance_bot

# Создайте .env файл с настройками
cp .env.example .env  # Если есть
nano .env

# Запустите
docker-compose up -d

# Проверьте
docker-compose ps
```

**Управление:**
```bash
docker-compose up -d|down|restart
docker-compose logs -f bot  # Просмотр логов
```

#### Вариант C: Screen/Tmux 📺

**Преимущества:**
- Простой в использовании
- Не требует дополнительной настройки
- Работает в фоне после отключения SSH

**Использование:**
```bash
screen -S family_bot
cd family_finance_bot
source venv/bin/activate
python main.py
# Нажмите Ctrl+A, затем D для отключения
```

**Управление:**
```bash
screen -ls                    # Список сессий
screen -r family_bot          # Подключиться
screen -X -S family_bot quit  # Закрыть сессию
```

### Шаг 3: Используйте готовые скрипты

После настройки любого метода, используйте универсальные скрипты:

```bash
./STATUS_BOT.sh     # Проверить статус
./VIEW_LOGS.sh      # Посмотреть логи
./RESTART_BOT.sh    # Перезапустить при необходимости
```

---

## 🤖 Настройка автоматизации

### Автоматическая проверка и перезапуск

```bash
# Откройте редактор cron
crontab -e

# Добавьте проверку каждые 5 минут
*/5 * * * * /home/user/finance_calculator/HEALTH_CHECK.sh >> /var/log/bot_health.log 2>&1
```

`HEALTH_CHECK.sh` будет:
- Проверять, работает ли бот
- Автоматически перезапускать при сбое
- Логировать все действия
- Отправлять уведомления о проблемах (если настроено)

### Автоматическое резервное копирование

Создайте скрипт backup:

```bash
nano backup_bot.sh
```

```bash
#!/bin/bash
BACKUP_DIR="$HOME/backups"
mkdir -p "$BACKUP_DIR"
cp family_finance_bot/family_finance.db "$BACKUP_DIR/backup_$(date +%Y%m%d).db"
# Удалить старше 30 дней
find "$BACKUP_DIR" -name "*.db" -mtime +30 -delete
```

```bash
chmod +x backup_bot.sh

# Добавьте в cron для ежедневного backup в 3:00
crontab -e
# 0 3 * * * /home/user/finance_calculator/backup_bot.sh
```

**Подробнее:** См. [AUTOMATION_SETUP.md](AUTOMATION_SETUP.md)

---

## 📊 Мониторинг

### Проверка статуса

```bash
./STATUS_BOT.sh
```

Покажет:
- Статус бота (работает/остановлен)
- Метод запуска (systemd/Docker/процесс)
- Использование ресурсов
- Информацию о базе данных
- Последние логи

### Просмотр логов

```bash
./VIEW_LOGS.sh
```

Интерактивное меню для выбора:
- Последние 50/100 строк
- Следить за логами в реальном времени
- Логи за определенный период
- Логи ошибок

### Проверка работоспособности

```bash
./HEALTH_CHECK.sh
```

Автоматически:
- Проверит, работает ли бот
- Попытается перезапустить при сбое
- Залогирует результаты
- Отправит уведомление (если настроено)

---

## 🔄 Обновление бота

### Безопасное обновление

```bash
# 1. Остановить бота
./STOP_BOT.sh

# 2. Создать backup
cp family_finance_bot/family_finance.db family_finance_bot/family_finance.db.backup

# 3. Обновить код
cd family_finance_bot
git pull origin development

# 4. Обновить зависимости
source venv/bin/activate
pip install -r requirements.txt

# 5. Применить миграции
alembic upgrade head

# 6. Запустить бота
cd ..
./START_BOT.sh

# 7. Проверить статус
./STATUS_BOT.sh
```

---

## 🆘 Решение проблем

### Бот не запускается

```bash
# Проверьте логи
./VIEW_LOGS.sh

# Проверьте конфигурацию
cat family_finance_bot/.env

# Проверьте базу данных
ls -la family_finance_bot/family_finance.db

# Попробуйте перезапустить
./RESTART_BOT.sh
```

### Бот не отвечает на команды

```bash
# Проверьте статус
./STATUS_BOT.sh

# Проверьте последние ошибки
tail -50 family_finance_bot/logs/errors.log

# Жесткий перезапуск
./STOP_BOT.sh
sleep 3
./START_BOT.sh
```

### Проблемы с базой данных

```bash
# Проверьте целостность
cd family_finance_bot
sqlite3 family_finance.db "PRAGMA integrity_check;"

# Проверьте версию миграций
alembic current

# Примените миграции
alembic upgrade head
```

### Диск заполнен

```bash
# Проверьте использование
df -h

# Очистите старые логи
sudo journalctl --vacuum-time=7d

# Очистите старые backup'ы
find ~/backups -name "*.db" -mtime +30 -delete

# Сжмите базу данных SQLite
cd family_finance_bot
sqlite3 family_finance.db "VACUUM;"
```

**Подробнее:** См. [VDS_MANAGEMENT_GUIDE.md](VDS_MANAGEMENT_GUIDE.md#решение-проблем)

---

## 📁 Структура файлов

```
finance_calculator/
├── VDS_MANAGEMENT_README.md       # Этот файл (главный обзор)
├── VDS_MANAGEMENT_GUIDE.md        # Подробное руководство
├── QUICK_REFERENCE.md             # Быстрая справка
├── CHEATSHEET_RU.md               # Краткая шпаргалка
├── AUTOMATION_SETUP.md            # Настройка автоматизации
│
├── START_BOT.sh                   # Запуск бота
├── STOP_BOT.sh                    # Остановка бота
├── RESTART_BOT.sh                 # Перезапуск бота
├── STATUS_BOT.sh                  # Проверка статуса
├── VIEW_LOGS.sh                   # Просмотр логов
├── HEALTH_CHECK.sh                # Автоматическая проверка
│
└── family_finance_bot/
    ├── main.py                    # Основной файл бота
    ├── family-finance-bot.service # Systemd service файл
    ├── docker-compose.yml         # Docker Compose конфигурация
    ├── Makefile                   # Makefile с командами
    └── ...
```

---

## 🎯 Рекомендуемая конфигурация для VDS

### Для стабильной работы 24/7:

1. **Метод запуска:** Systemd Service
2. **Автозапуск:** Включен (`systemctl enable`)
3. **Health Check:** Каждые 5 минут через cron
4. **Backup:** Ежедневно в 3:00 через cron
5. **Ротация логов:** Еженедельно (хранить 30 дней)
6. **Мониторинг:** Health check с уведомлениями

### Команды для настройки:

```bash
# 1. Настройте systemd service
cd family_finance_bot
sudo cp family-finance-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable family-finance-bot
sudo systemctl start family-finance-bot

# 2. Настройте автоматизацию
cd ..
chmod +x *.sh
crontab -e
# Добавьте:
# */5 * * * * /home/user/finance_calculator/HEALTH_CHECK.sh >> /var/log/bot_health.log 2>&1
# 0 3 * * * cp /home/user/finance_calculator/family_finance_bot/family_finance.db /home/user/backups/backup_$(date +\%Y\%m\%d).db

# 3. Проверьте работу
./STATUS_BOT.sh
```

---

## 💡 Полезные советы

### Добавьте alias в ~/.bashrc

```bash
echo "# Family Finance Bot aliases" >> ~/.bashrc
echo "alias bot-status='cd ~/finance_calculator && ./STATUS_BOT.sh'" >> ~/.bashrc
echo "alias bot-logs='cd ~/finance_calculator && ./VIEW_LOGS.sh'" >> ~/.bashrc
echo "alias bot-restart='cd ~/finance_calculator && ./RESTART_BOT.sh'" >> ~/.bashrc
source ~/.bashrc
```

Теперь можно использовать: `bot-status`, `bot-logs`, `bot-restart` из любой директории.

### Настройте уведомления

Отредактируйте `HEALTH_CHECK.sh` и добавьте отправку уведомлений:
- Email (через mailutils)
- Telegram (через API)
- Slack (через webhook)

**Подробнее:** См. [AUTOMATION_SETUP.md](AUTOMATION_SETUP.md#уведомления-о-проблемах)

### Регулярное обслуживание

Рекомендуется:
- Проверять логи раз в неделю
- Проверять backup'ы раз в месяц
- Обновлять бота при выходе новых версий
- Мониторить использование диска

---

## 📞 Поддержка и помощь

### Документация
- Все руководства находятся в корне проекта
- Используйте `grep` для поиска по документам:
  ```bash
  grep -r "нужная тема" *.md
  ```

### Логи
- Systemd: `sudo journalctl -u family-finance-bot -f`
- Docker: `docker-compose logs -f bot`
- Файлы: `family_finance_bot/logs/`

### Диагностика
- Запустите `./STATUS_BOT.sh` для общего обзора
- Запустите `./VIEW_LOGS.sh` для просмотра логов
- Проверьте `family_finance_bot/logs/errors.log` для ошибок

---

## 🔒 Безопасность

### Рекомендации:

1. **Защитите конфиденциальные файлы:**
   ```bash
   chmod 600 family_finance_bot/.env
   chmod 600 family_finance_bot/family_finance.db
   ```

2. **Используйте отдельного пользователя для бота:**
   ```bash
   sudo useradd -r -s /bin/false botuser
   sudo chown -R botuser:botuser /opt/family-finance-bot
   ```

3. **Настройте firewall:**
   ```bash
   sudo ufw enable
   sudo ufw allow ssh
   # Откройте только необходимые порты
   ```

4. **Регулярные backup'ы:**
   - Храните backup'ы в отдельном месте
   - Тестируйте восстановление из backup
   - Используйте шифрование для чувствительных данных

5. **Обновления системы:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

---

## 📈 Метрики и статистика

### Полезные команды для мониторинга:

```bash
# Размер базы данных
du -h family_finance_bot/family_finance.db

# Количество пользователей и расходов
cd family_finance_bot
sqlite3 family_finance.db "SELECT COUNT(*) FROM users;"
sqlite3 family_finance.db "SELECT COUNT(*) FROM expenses;"

# Время работы бота (systemd)
systemctl show family-finance-bot -p ActiveEnterTimestamp

# Использование памяти
ps aux | grep main.py | awk '{print $4"%", $6/1024"MB"}'
```

---

## 🎓 Дополнительное обучение

Рекомендуется изучить:
- Основы systemd для управления сервисами
- Docker и Docker Compose для контейнеризации
- Cron для автоматизации задач
- Базовый мониторинг Linux серверов

**Полезные ресурсы:**
- `man systemctl` - документация systemd
- `man crontab` - документация cron
- `docker-compose --help` - помощь по Docker Compose

---

## ✨ Что дальше?

После настройки базового управления:

1. ✅ **Настройте мониторинг** - [AUTOMATION_SETUP.md](AUTOMATION_SETUP.md)
2. ✅ **Добавьте уведомления** о проблемах
3. ✅ **Настройте резервное копирование** в облако
4. ✅ **Оптимизируйте производительность** БД
5. ✅ **Настройте метрики** для анализа работы

---

## 📊 Чеклист для продакшена

- [ ] Systemd service настроен и запущен
- [ ] Автозапуск включен
- [ ] Health check работает в cron
- [ ] Backup настроен и тестирован
- [ ] Логи ротируются автоматически
- [ ] Уведомления о проблемах настроены
- [ ] Права доступа к файлам правильные
- [ ] Firewall настроен
- [ ] Обновления системы автоматические
- [ ] Мониторинг диска настроен
- [ ] Документация изучена
- [ ] Скрипты протестированы

---

**Версия:** 1.0  
**Дата:** 22.11.2025  
**Автор:** Family Finance Bot Team

**Начните с:** [CHEATSHEET_RU.md](CHEATSHEET_RU.md) для быстрой справки или [VDS_MANAGEMENT_GUIDE.md](VDS_MANAGEMENT_GUIDE.md) для подробного руководства.

---

## 📝 Обратная связь

Если вы нашли ошибку или хотите предложить улучшение:
1. Создайте issue в репозитории
2. Или отредактируйте документацию напрямую

**Удачной работы с ботом! 🚀**

