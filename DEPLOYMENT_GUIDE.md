# 🚀 Руководство по развертыванию новых возможностей

## 📋 Предварительные требования

- Python 3.11+
- SQLite или PostgreSQL
- python-telegram-bot 20.0+
- Все зависимости из `requirements.txt`

---

## 🔧 Шаг 1: Обновление кода

### 1.1 Проверьте текущую версию

```bash
cd /Users/kirillterskih/finance\ calculator/family_finance_bot
git status
```

### 1.2 Обновите виртуальное окружение (если нужно)

```bash
source venv/bin/activate  # или source ../venv/bin/activate
pip install -r requirements.txt
```

---

## 🗄️ Шаг 2: Применение миграций базы данных

### 2.1 Проверьте текущую версию БД

```bash
python3 -m alembic current
```

### 2.2 Примените миграции

```bash
python3 -m alembic upgrade head
```

**Ожидаемый вывод:**
```
INFO  [alembic.runtime.migration] Running upgrade ec4db506fd7f -> bbb92a37ef54, add_user_settings_fields
```

### 2.3 Проверьте успешность миграции

```bash
python3 -m alembic current
```

Должно показать:
```
bbb92a37ef54 (head)
```

---

## ✅ Шаг 3: Проверка изменений

### 3.1 Проверьте структуру базы данных

```bash
sqlite3 family_finance.db ".schema users"
```

Должны появиться новые поля:
- `currency`
- `timezone`
- `date_format`
- `daily_summary_enabled`
- `daily_summary_time`

### 3.2 Проверьте новые файлы

```bash
ls -la bot/handlers/help.py
ls -la bot/handlers/settings.py
ls -la bot/handlers/family_settings.py
ls -la bot/handlers/middleware.py
ls -la bot/utils/keyboards.py
```

Все файлы должны существовать.

---

## 🧪 Шаг 4: Тестирование

### 4.1 Запустите бота

```bash
python3 main.py
```

**Ожидаемый вывод:**
```
Family Finance Bot Starting
Debug mode: False
Log level: INFO
Database: sqlite:///family_finance.db
Bot is running. Press Ctrl+C to stop.
```

### 4.2 Проверьте основные команды

В Telegram боте попробуйте:

1. **Тест главного меню:**
   ```
   /start
   ```
   - Должны появиться inline-кнопки
   - Проверьте кнопку "⚙️ Настройки"

2. **Тест помощи:**
   ```
   /help
   ```
   - Должно открыться меню помощи с категориями
   - Попробуйте навигацию по разделам

3. **Тест настроек:**
   ```
   /settings
   ```
   - Проверьте текущие значения настроек
   - Измените валюту
   - Проверьте, что изменение сохранилось

4. **Тест настроек семьи:**
   ```
   /family_settings
   ```
   - Если вы админ, должны появиться все опции
   - Попробуйте изменить название семьи

5. **Тест добавления расхода:**
   ```
   /add_expense или кнопка "➕ Добавить расход"
   ```
   - После успешного добавления должны появиться кнопки быстрых действий

---

## 🔍 Шаг 5: Проверка логов

### 5.1 Мониторинг работы бота

Следите за выводом в терминале:

```bash
# Должны появляться сообщения типа:
# INFO - User 123456789 opened settings
# INFO - Updated settings for user 1: {'currency': '$'}
# INFO - User 123456789 requested help
```

### 5.2 Проверка обработки ошибок

Попробуйте вызвать ошибку (например, отправьте некорректные данные):
- Должно появиться красивое сообщение об ошибке
- В логах должна быть детальная информация

---

## 🚨 Решение проблем

### Проблема 1: Ошибка при миграции

**Ошибка:**
```
NotImplementedError: No support for ALTER of constraints in SQLite
```

**Решение:**
Файл миграции уже исправлен. Если ошибка повторяется:

```bash
# Откатите миграцию
python3 -m alembic downgrade -1

# Проверьте файл миграции
cat alembic/versions/bbb92a37ef54_add_user_settings_fields.py

# Примените снова
python3 -m alembic upgrade head
```

### Проблема 2: Импорт не найден

**Ошибка:**
```
ImportError: cannot import name 'get_main_menu_keyboard' from 'bot.utils.keyboards'
```

**Решение:**

```bash
# Проверьте наличие файла
ls -la bot/utils/keyboards.py

# Если файла нет, создайте его заново
# (код файла в репозитории)
```

### Проблема 3: Бот не запускается

**Ошибка:**
```
TypeError: 'NoneType' object is not callable
```

**Решение:**

```bash
# Проверьте bot/__init__.py
# Убедитесь, что все handlers импортированы правильно

# Проверьте наличие всех необходимых файлов
ls -la bot/handlers/*.py
```

### Проблема 4: Настройки не сохраняются

**Решение:**

```bash
# Проверьте права доступа к БД
ls -la family_finance.db

# Проверьте логи на предмет ошибок при commit
# Убедитесь, что session.commit() вызывается
```

---

## 📊 Шаг 6: Проверка производительности

### 6.1 Мониторинг запросов к БД

Добавьте в `config/settings.py`:

```python
# Для отладки SQL-запросов
SQLALCHEMY_ECHO = True  # Только для разработки!
```

### 6.2 Проверка времени ответа

Middleware автоматически логирует время выполнения операций:

```
WARNING - Request failed after 2.35 seconds
```

---

## 🔐 Шаг 7: Безопасность (для продакшена)

### 7.1 Проверьте настройки

```python
# config/settings.py

DEBUG = False  # Выключите debug в продакшене
LOG_LEVEL = "INFO"  # Или "WARNING" для меньшего количества логов
```

### 7.2 Защита базы данных

```bash
# Установите правильные права доступа
chmod 600 family_finance.db

# Для PostgreSQL используйте пароли и SSL
```

### 7.3 Backup базы данных

```bash
# Создайте резервную копию перед миграцией
cp family_finance.db family_finance.db.backup.$(date +%Y%m%d)

# Автоматический backup (добавьте в cron)
0 2 * * * cp /path/to/family_finance.db /path/to/backups/family_finance.db.$(date +\%Y\%m\%d)
```

---

## 📈 Шаг 8: Мониторинг после развертывания

### 8.1 Первые 24 часа

Следите за:
- Количеством ошибок в логах
- Временем ответа бота
- Использованием новых команд

### 8.2 Метрики

Отслеживайте:
- Количество вызовов `/settings`
- Количество вызовов `/family_settings`
- Количество вызовов `/help`
- Частоту использования inline-кнопок

### 8.3 Обратная связь

Соберите feedback от пользователей:
- Удобство новых функций
- Обнаруженные баги
- Предложения по улучшению

---

## 🎉 Готово!

Новые функции успешно развернуты!

### Что дальше:

1. **Мониторинг:** Следите за логами и метриками
2. **Feedback:** Собирайте отзывы пользователей
3. **Оптимизация:** При необходимости оптимизируйте запросы
4. **Документация:** Обновите пользовательскую документацию

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `tail -f logs/bot.log`
2. Проверьте документацию: `UX_IMPROVEMENTS_RELEASE_NOTES.md`
3. Проверьте issues на GitHub (если используется)

---

**Версия:** 1.1.0  
**Дата:** 15.11.2025  
**Автор:** Family Finance Bot Team

