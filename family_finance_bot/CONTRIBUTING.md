# Руководство по внесению вклада

Спасибо за интерес к проекту Family Finance Bot! Мы рады любому вкладу в развитие проекта.

## 🚀 Как начать

1. **Fork репозитория**
   - Создайте fork на GitHub
   - Клонируйте свой fork локально

2. **Настройка окружения**
   ```bash
   cd family_finance_bot
   python -m venv venv
   source venv/bin/activate
   make install
   ```

3. **Создайте ветку для изменений**
   ```bash
   git checkout -b feature/your-feature-name
   ```

## 📝 Стандарты кода

### Python Style Guide

- Следуйте [PEP 8](https://pep8.org/)
- Используйте type hints для всех функций
- Максимальная длина строки: 100 символов
- Используйте docstrings для документации

### Форматирование кода

```bash
# Автоматическое форматирование
make format

# Проверка линтером
make lint

# Проверка типов
make type-check
```

### Пример кода

```python
"""Module docstring."""

from typing import Optional

from telegram import Update
from telegram.ext import ContextTypes


async def example_handler(
    update: Update, 
    context: ContextTypes.DEFAULT_TYPE
) -> Optional[int]:
    """Handle example command.
    
    Args:
        update: Telegram update object.
        context: Telegram context object.
        
    Returns:
        Optional conversation state.
    """
    if not update.message:
        return None
        
    await update.message.reply_text("Example response")
    return None
```

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
make test

# С покрытием
make test-cov

# Конкретный файл
pytest tests/test_specific.py
```

### Написание тестов

- Каждая новая функция должна иметь тесты
- Покрытие кода должно быть не менее 80%
- Используйте pytest fixtures для общих настроек
- Тесты должны быть изолированными

Пример теста:

```python
import pytest

from bot.utils.decorators import admin_only


@pytest.mark.asyncio
async def test_admin_decorator(mock_update, mock_context):
    """Test admin_only decorator."""
    @admin_only
    async def protected_handler(update, context):
        return "success"
    
    result = await protected_handler(mock_update, mock_context)
    assert result == "success"
```

## 📚 Документация

### Комментарии в коде

- Используйте docstrings в формате Google
- Комментируйте сложную логику
- Обновляйте README.md при добавлении новых функций

### Коммиты

Следуйте [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: добавить новую команду /balance
fix: исправить ошибку в обработке транзакций
docs: обновить README с примерами
test: добавить тесты для модуля database
refactor: улучшить структуру handlers
style: форматировать код согласно PEP8
chore: обновить зависимости
```

## 🔄 Pull Request Process

1. **Обновите документацию**
   - README.md (если нужно)
   - Docstrings в коде
   - CHANGELOG.md

2. **Убедитесь, что все проверки проходят**
   ```bash
   make format
   make lint
   make type-check
   make test
   ```

3. **Создайте Pull Request**
   - Опишите изменения
   - Укажите связанные issues
   - Добавьте скриншоты (для UI изменений)

4. **Ожидайте review**
   - Отвечайте на комментарии
   - Вносите необходимые изменения

### Шаблон Pull Request

```markdown
## Описание
Краткое описание изменений

## Тип изменения
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Чеклист
- [ ] Код соответствует style guide
- [ ] Добавлены/обновлены тесты
- [ ] Все тесты проходят
- [ ] Документация обновлена
- [ ] Нет предупреждений от линтера
```

## 🐛 Сообщения об ошибках

### Перед созданием issue

- Проверьте существующие issues
- Убедитесь, что ошибка воспроизводится
- Соберите информацию о системе

### Шаблон issue

```markdown
**Описание ошибки**
Четкое описание проблемы

**Шаги для воспроизведения**
1. Перейти к '...'
2. Нажать на '...'
3. Увидеть ошибку

**Ожидаемое поведение**
Что должно было произойти

**Скриншоты**
Если применимо

**Окружение:**
 - OS: [e.g. macOS, Linux]
 - Python version: [e.g. 3.11]
 - Bot version: [e.g. 0.1.0]

**Дополнительный контекст**
Любая другая информация
```

## 💡 Предложение функций

### Шаблон feature request

```markdown
**Описание функции**
Четкое описание желаемой функции

**Мотивация**
Почему эта функция нужна?

**Предлагаемое решение**
Как это должно работать?

**Альтернативы**
Какие еще варианты вы рассматривали?

**Дополнительный контекст**
Скриншоты, примеры и т.д.
```

## 🏗️ Структура проекта

Понимание структуры поможет вам быстрее ориентироваться:

```
bot/
├── database/      # Модели и работа с БД
├── handlers/      # Обработчики команд
└── utils/         # Вспомогательные функции

config/            # Конфигурация приложения
tests/             # Тесты
alembic/           # Миграции БД
```

## 📋 Чек-лист разработчика

- [ ] Код написан и протестирован локально
- [ ] Добавлены type hints
- [ ] Написаны docstrings
- [ ] Добавлены unit тесты
- [ ] Тесты проходят (`make test`)
- [ ] Код отформатирован (`make format`)
- [ ] Линтер не выдает ошибок (`make lint`)
- [ ] Проверка типов пройдена (`make type-check`)
- [ ] Документация обновлена
- [ ] Коммиты следуют Conventional Commits
- [ ] Pull Request создан с подробным описанием

## 🤝 Code of Conduct

- Будьте уважительны к другим участникам
- Конструктивная критика приветствуется
- Помогайте новичкам
- Следуйте стандартам проекта

## 📞 Связь

- **Issues**: Для багов и предложений
- **Discussions**: Для вопросов и обсуждений
- **Pull Requests**: Для отправки кода

Спасибо за ваш вклад! 🎉

